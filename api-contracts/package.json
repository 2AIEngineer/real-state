{
  "name": "@real-state/api-contracts",
  "version": "1.0.0",
  "private": true,
  "description": "Zod schemas and TypeScript types of the backend API, kept in sync with its OpenAPI schema.",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "node --import tsx --test tests/*.test.ts"
  },
  "peerDependencies": {
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "@types/node": "^26.6.2",
    "tsx": "^4.23.15",
    "typescript": "^5.6.0",
    "zod": "^4.0.0"
  }
}
